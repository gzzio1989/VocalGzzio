# VocalGzzio v2.9.0 変更点 — セッションモード

前バージョン: v2.8.1（公開済み）。

この回は **オンラインセッション（SYNCROOM など）で使うため**の回です。
「低遅延」ボタンを**廃止**して、**追加遅延を0サンプルに固定する「セッション」スイッチ**に
置き換えました。あわせて、調べている途中で見つかった**遅延の申告バグを1件**直しています。

---

## なぜ「低遅延」を捨てたのか

旧「低遅延」ボタンは、ピッチ系の遅延を **768 → 384 サンプル（17.4 → 8.7ms）** に
半分にするものでした。分析の窓を縮めるので、低い声はわずかにざらつきます。

ところが、オンラインセッションで快適とされる **往復30ms** は、
**ネットとパソコンを通した合計の実測値**です。自分のパソコンの持ち分はその半分も残りません。
オーディオインターフェースのAD/DAとバッファだけで7〜8ms使うので、
**プラグインに許される追加遅延は実質ゼロ**です。

つまり 8.7ms は「半分になった」のではなく「**まだ足りない**」でした。
音質を犠牲にして、足りない答えを出していたことになります。

→ **「減らす」をやめて「足さない」を保証する**構えに変えました。

---

## セッションモード

ヒーロー帯（画面上部）の右端に、**「追加遅延」バッジ**と**「セッション」スイッチ**を置きました。
**かんたん／こだわりのどちらでも同じ位置**に出るので、セッション中にモードを行き来しても
迷子になりません。

### ONの間なにが起きるか

遅延をふやす3つ（**ピッチ補正・ボイス変換・ハモリ／5人ユニゾン**）を素通しにします。
スイッチの値そのものは変えないので、**セッションを抜ければ元どおり鳴ります**。

止めている間、その3つのツマミは**灰色**になります。v2.8.0で直した
「見えないのに効いている」の逆、**「見えているのに効いていない」**を起こさないためです。

### 追加遅延バッジ

いま何ms足しているかを**常に**出します。0なら緑、足していれば黄色。

「遅延ゼロ」は宣伝文句ではなく測った数字だ、というのがこの製品の芯なので、
その数字を隠さずに画面へ出すことにしました。**再生中にピッチ補正をONにすると
その場で +17.4 ms に変わります**（申告値ではなく、いま実際に遅れている値を出しています。
歌う人が知りたいのは「自分の声が何ms遅れて返ってくるか」だからです）。

---

## 直したバグ: ハモリだけONにすると、歌だけ17.4ms走っていた

調べている途中で見つかりました。**ハモリ（5人ユニゾン）だけをON**にすると、

- プラグインはホストへ **「768サンプル遅れます」と申告**していた
- ところが実測すると、主メロのピークは **0サンプル**（＝まったく遅れていない）

ハモリは主メロの**横に**声を足す並列処理なので、主メロ自体は遅れません。
申告だけが17.4ms嘘をついていたことになります。実害は2つ:

1. **DAWがその嘘を信じてトラックを17.4ms前へ引っ張る** → 歌だけ走る
2. **Mixの原音そろえも同じ値を見ていた** → 原音だけ17.4ms遅れて混ざり、
   v2.8.0で直したはずの**コムフィルタ（スカスカ）が、ハモリのときだけ再発**していた

主メロが実際に遅れるのは**ボイス変換／ピッチ補正**のときだけなので、
申告の条件からハモリを外しました。

---

## 確認したこと

`tools/bench_lat.cpp`（新規／`bash tools/run_bench_lat.sh` で再実行）で、
機能ごとに1本ずつ遅延を測りました。
44.1kHz / 124サンプル（グッジオさんの実環境と同じ、締切2.81ms）。

| 場面 | 申告 | 実測 |
|---|---|---|
| 出荷状態 | 0 | **0** |
| セッション構成（ジー音/ことば/圧縮/艶/自動音量/軽リバーブ） | 0 | **0** |
| ＋やまびこ・コーラス | 0 | **0** |
| 空間ぜんぶ（ひろがり/かさね/シマー/リング まで） | 0 | **0** |
| **ハモリ／5人ユニゾンのみ** | **0**（旧768＝バグ） | **0** |
| ハモリ＋ピッチ補正 | 768 | **768** |
| こぶしのみ（補正OFF） | 0 | **0** |
| ピッチ補正80 | 768 | **768** |
| ボイス変換のみ（+3半音） | 768 | 943（±1ホップ内＝正常） |
| **★セッションON ＋ ピッチ補正80** | **0** | **0** |
| **★セッションON ＋ 全部盛り** | **0** | **0** |

セッション構成の負荷は **平均 5.5% / 99%点 7.5% / 最悪 11.0%**（締切=100%）。

### 実起動で目で確認したこと

- 「追加遅延 +0.0 ms」バッジと「セッション」スイッチが、ヒント文にもプルダウンにも
  重なっていない（v2.8.0のテンポフィットの反省）
- セッションONで、ピッチ補正・ボイス変換・ハモリ関係のツマミが灰色になる
- **ONにしたときのボタンの文字が読める**
  （最初の実装では暗い地に暗い文字が乗って読めず、スクショで気づいて直しました）
- 左上のバージョン表示が **v2.9.0**

---

## 音が変わるかどうか

**セッションモードをONにしなければ、設定を変えていない限り変わりません。**

例外はハモリです。ハモリだけを使っていた場合、Mixで混ぜ戻す原音のそろえ方が直るので、
**Mixを中間にしていた人はスカスカが解消します**（直った方向の変化です）。

旧「低遅延」をONにして保存していたプロジェクトは、ボタンが無くなったぶん
**通常の窓（768サンプル）に戻ります**。低い声のざらつきは減り、遅延は17.4msになります。
セッションで使っていた方は、かわりに**セッションスイッチ**を使ってください（0msになります）。

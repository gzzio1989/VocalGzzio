# GitHub リリース v2.8.0 用の本文

タイトル: `v2.8.0 — つかいやすさと安定性（18件の修正）＋ こぶし`

---

新機能ではなく、**ひと回り点検して見つかった 18 件**を直した回です。
v2.7.0 の新機能「こぶし」（しゃくり・こぶし保護）も入っています。

## いちばん大きい3つ

### マイク音量が、本当にいちばん手前で効きます

説明文には「ぜんぶの処理のいちばん手前で掛ける」と書いてあったのに、実際はゲート・
ノイズ除去・ボイス変換のあと、しかも **IN メーターを取ったあと**に掛けていました。
そのため **マイク音量を上げても IN メーターが動かない**、ゲートのしきい値が入力に
追いつかない、という状態でした。位置を直しました。

### かんたんモードの「見えないのに効いている」を無くしました

ボイス変換やロボ声のツマミはこだわりモードにしかありません。かんたんモードへ切り替えると
その欄が消えるだけで**音には効いたまま**だったので、「声が変なままなのに直す場所が無い」
状態になっていました。効いているときだけ帯を出し、**ひと押しで素の声に戻せる**ように
しました。何も効いていないときは出ません。

### ツマミをダブルクリックで初期値に戻せます

DAW ではほぼ共通の操作なのに効きませんでした。クリック後に矢印キーでも動きます。

## 音声コールバックの中でメモリを確保しなくなりました

`IIR::Coefficients::makeXxx()` は中身が `return *new Coefficients(...)` です。これを
1ブロックに11回＋ディエッサー/スマートEQで32サンプルごとにも呼んでいたので、64サンプルの
バッファでは **1.3ms ごとに数十回の malloc/free** が起きていました。「配信中つけっぱなし」
を謳う製品としては致命的で、プツッと切れる原因になります。確保の起きない
`ArrayCoefficients` へ全部置き換えました。実測 **1000回 → 0回**、係数は1ビットも
変わらないので**音は同じ**です。

## 処理（音）の修正

- **ボイス変換ON＋Mixを中間にするとスカスカ** … 加工側は約16ms遅れるのに遅れていない
  原音を混ぜていた＝16msのコムフィルタ。原音も同じだけ遅らせてから混ぜます
- **ゲートONだとノイズ除去が効かない** … ノイズの床をゲート後の音で測っていたので、
  フレーズの合間に床が最小値まで落ちていました。ゲートが閉じている間は学習を止めます
- ハモリの基準ピッチが 440Hz 固定だった（主メロだけ基準ピッチに従い、常にうなっていた）
- **ひろがり／かさねを上げるとステレオが潰れる**（5人ユニゾンの広がりが消える）
- シマーで配列の外に書き込む可能性（大きなブロックでクラッシュし得た）
- **A/Bの片方が保存されない**（保存して開き直すとBが空になり、押しても無反応）
- こぶしの効きがバッファサイズで変わっていた（時間を実時間で数えるようにした）
- 体験版のカウンタが約12.4時間で溢れ、計算上 +50dB の爆音になり得た

## 画面の修正

- **「テンポフィット」が押せなかった** … 「ひびきの種類」プルダウンの下敷きで、110pxの
  うち108pxが隠れていました。押すとプルダウンが開いていました
- テーマを押すとEQグラフがかんたんモードに出てくる／見えないFFTが30Hzで回り続ける
- 「こだわりでスマートEQを手動→かんたんへ」で6つのツマミが宙に浮いて復活する
- 英語UIのディレイ音符名が1つずつ後ろにずれていた（「1/8 dotted」で4分音符が鳴る）
- こだわりモードで音名(42px)が13pxの枠を突き抜けていた
- テーマ帯の半透明の膜が「文字」「大きさ」スライダーを覆って読みにくかった
- **文字サイズの数値が「1…」に切れて読めなかった**（文字を大きくする操作なのに）
- 文字サイズは150%まであるのに数値入力は120%で止まっていた
- 音域チェック中、こだわりモードでグラフが10pxに潰れて文字が団子になっていた

## 確認したこと

`tools/run_tests.sh` で **89項目 PASS**。今回ぶんの実測値は次のとおりです。

| 内容 | 結果 |
|---|---|
| Mixそろえ前 31.25Hz（コムの谷） | **-177.0 dB**（バグの再現） |
| Mixそろえ後（バッファ64/128/512） | 差 **0.05 / 0.03 / 0.05 dB** |
| 係数作り直し1000回のメモリ確保（前／後） | **1000回 → 0回** |
| 係数の中身の差 | **0**（音は変わらない） |
| こぶし: バッファ128と4096の一致 | **0.875 / 0.921** |

実起動でも、かんたん／こだわりを行き来して「出荷状態では警告が出ない／ボイス変換ONで
出る／ひと押しで消える」まで目で確認しています。

---

**設定を変えていなければ音は変わりません。** Mix・ハモリ・ステレオはその機能を使って
いるときだけ、いずれも直った方向に変わります。ノイズ除去はゲートONで使っていた方には
効きが戻る形で変わります。

動作環境: Windows 10 / 11 (64bit)、VST3 または スタンドアロン
未署名のため、初回起動時に SmartScreen の警告が出ることがあります
（「詳細情報」→「実行」で進めてください）。

# v2.5.0 なめらか・マイク音量・音量キープ・10秒おまかせ

※ v2.4.0（かんたんモードの作り直し）は 2026-07-27 にリリース済み。その後に足した新機能を v2.5.0 として切り出した。

## 新機能「なめらか」（動的レゾナンス抑制）

### 何をするか
900Hz〜9kHz を24バンド（対数等分）で常時見張り、**近傍バンドの地ならし線より出っ張った帯域だけ**を、出た瞬間だけ削る。音程が動けば削る場所も追いかける。「3 音色をつくる」の先頭、パラメータIDは `res_amt`（0〜100%、初期0%）。

### 仕組み（Source/Resonance.h、JUCE非依存）
- **ゼロ遅延の作り方**: FFT方式(soothe等)は窓ぶんの遅延が出る。代わりに Regalia–Mitra 型の**2次オールパス1本**を各バンドに置く。`bandpass = 0.5*(x - allpass(x))`、`notch = x - (1-K)*bandpass`。**K は係数でなくただの掛け算**なので、サンプル単位で動かしても係数の再構築が無い。
- **出っ張りの判定**: バンド包絡(attack 5ms / release 50ms)の対数を、近傍バンドの平均(地ならし線)と比較。**自分と両隣は地ならし線に入れない**（レゾナンスの裾で両隣も持ち上がり、基準ごと上がって出っ張りが小さく見えるため。これに気づくまでカットが1dBしか出なかった）。
- **削り方**: 超過が1.8dBを超えたぶんをそのまま削る(上限10dB)。ゲインは削る方向8ms/戻す方向120msで毎サンプル追従。左右は同じゲイン（定位が動かないように）。
- **下限を900Hzで切る理由**: 低域では倍音間隔(男声100〜150Hz)よりバンド幅が狭くなり、**普通の倍音1本1本が出っ張りに見えて**声の芯を削ってしまう。900Hz以上なら倍音が密に並ぶので誤検出しない。低域のボワつきは既存「こもり」の守備範囲。
- **信号経路**: こもり・キンキン(固定EQ)の後、コンプの前。コンプより先に刺さりを削らないと、コンプが刺さりごと持ち上げるため。

### 検証（tools/dsp_resonance.cpp — 本物の Resonance.h を #include して試験）
| 項目 | 結果 | 合格ライン |
|---|---|---|
| +12dBの刺さり(3.1kHz)のカット | **4.9 dB** | ≥4 |
| 離れた基準帯域(1kHz)への影響 | 0.30 dB | <1 |
| きれいな女声(ビブラート付き)のRMS変化 | 0.05 dB / 最大カット1.6dB | <0.7 / <3 |
| レゾナンス移動後の追従(4.3kHz) | 6.1 dB | ≥3 |
| ステレオ10秒の処理時間 | 実時間の1.8% | <3% |

### チューニングの経緯（次に触る人へ）
初版は「カット1.16dB」しか出なかった。原因は2つ: (1)地ならし線に両隣を含めていて、裾で持ち上がった両隣が基準を引き上げていた → ±2〜±6バンドだけで基準を作る。(2)しきい値3dB・比率0.75では、刺さりが2バンドにまたがったとき片方ずつの超過が小さく割れて効かない → しきい値1.8dB・比率1.0・検出帯域幅を隣接間隔の1.25倍に。この3点で全テスト合格。**しきい値を下げすぎると「きれいな声への最大カット」が3dBを超えて声が痩せ始める**ので、変更時は必ず `./dsp_resonance` を回すこと。

## 新機能「マイク音量」「音量キープ」（グッジオさん指摘への対応）

「かんたんモードにマイク音量が無い」の指摘から。調べると既存の「仕上げ音量」は 0〜+18dB で**下げられない**ため、入力段に新設した。

### マイク音量（in_gain, ±24dB, 初期0）
全処理のいちばん手前で掛ける入力トリム。15msでなめして急変時のジッパーノイズを防ぐ。ゲート/コンプのしきい値がマイクの個体差に左右されなくなる。かんたんモードの先頭・こだわりの「2 音量をそろえる」先頭に表示。INメーターは加工前を指したままなので「INが小さい→マイク音量を上げる」という導線になる。

### 音量キープ（ride_amt, 0〜100%, 初期0）= Vocal Rider 相当
「普通のモードにVocalRider的なものはあるの？」への回答。ならし圧縮はコンプ＝速い波の担当で、フレーズ単位のゆっくりした差は残っていた。300msのRMSを見て目標-18dBFSへ±9dBの範囲でゲインを追従（時定数700ms=人がフェーダーを触る速さ）。**-45dBFS未満ではゲイン凍結**＝無音・息つぎでノイズを持ち上げない。位置はコンプ/ディエッサーの後・追加EQの前。ただの掛け算なのでゼロ遅延。
- **検証(tools/dsp_ride.cpp)**: フレーズ差10.6dB→0.9dB / 補正±9dB以内 / 無音区間(-70dBノイズ)は-69dBのまま。
- 補足: 目標まで9dB以上足りない極端な小声は可動域の上限で頭打ちになる（仕様）。テスト初版はここを見落として「不合格」と誤判定した。

## うた自動の10秒ワンボタン化（グッジオさん指定）
かんたんモードでは「うた自動」を画面中央に大きく置き（340×56px・文字20px、`"fontH"`プロパティで`getTextButtonFont`を上書き）、**1回押すだけで LEARN＋うた自動を10秒で終わらせる**。

内訳: 0〜0.5秒=間（押した直後の物音を拾わないための猶予）→ 0.5〜2.0秒=ノイズ学習（`requestDenoiseLearn()`、中身は1.5秒でLEARNと同一）→ 2.0〜10.0秒=`requestAutoSetup(1)`（8秒のうた解析）。

実装はエディタの20Hzタイマーで進める小さな状態機械（`easyComboPhase` 0=待機/1=しずかに/2=解析中、`easyComboTick`）。**音声スレッドには何も足していない**（既存の2つの仕組みを時間差で呼ぶだけ）。フェーズ1の間はボタンが「しずかにしてね… あと N秒」、フェーズ2は従来の「解析中… あと N%」。カウントダウン中にこだわりへ切り替えたら中断（解析まで進んでいたら続行）。こだわりモードのうた自動は従来どおり8秒単独。


## かんたんモードの変更
マイク音量が加わり **7ツマミ → 8ツマミ** に（先頭がマイク音量）。それ以外の見た目はリリース済み v2.4.0 のまま。

## 検証
- Linuxフルビルド エラー0 / Xvfb実起動で かんたん(8ツマミ)・こだわり(2音量=7ノブ, 3音色=6ノブ) 両方をスクショ確認。
- 10秒おまかせはクリック実測で「しずかにしてね…→解析中…」の遷移を確認（検証環境に音声デバイスが無いため、解析の完了だけは実機確認が必要）。
- DSPは tools/dsp_resonance.cpp / tools/dsp_ride.cpp が全項目合格。

## 残り（v2.6.0候補）
ハムノイズ50/60Hz自動除去 / 子音エンハンサー（提案書参照）。

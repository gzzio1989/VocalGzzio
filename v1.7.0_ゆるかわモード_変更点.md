# VocalGzzio v1.7.0 変更点

## 1. 画面モード ― ヘッダーの十字スイッチ（5種）
十字スイッチのセルで UI モードを切替（相互排他・即時反映・選択は保存され次回復元）。

| 位置 | モード | 配色 | グラフ/アニメ | 用途 |
|------|--------|------|----------------|------|
| 中央 | **標準** | ダーク | あり | 従来の「時計職人」配色（既定） |
| 左 | **ゆるふわ** | パステル | あり（＋演出） | マスコット・ジュースノブ・水彩背景・泡アニメ |
| 右 | **軽量** | ダーク | **なし** | スペクトラム/演出を描かない最小構成（低負荷） |
| 上 | **ライト** | ライト | あり | 明るく読みやすい配色（白基調） |
| 下 | **ゆるふわ軽量** | パステル | **なし** | 可愛い見た目のまま低負荷 |

- 「軽量」「ゆるふわ軽量」は**どのモードからでも十字クリックで切替可能**。
- モードは `prefs`（共有ファイル）に保存 → `restoreUiState()` で復元。

## 2. 負荷とリアルタイム性（重点対応）
- **EQグラフ（スペクトラム）は表示中のみ FFT を回す**ように改善（`EQGraph::visibilityChanged`
  でタイマー開始/停止）。従来は非表示（エフェクトタブ等）でも 30Hz で 4096点FFT＋再描画を
  し続けていた。→ **軽量モードだけでなく通常時も CPU 削減**。
- 軽量モード（右・下）は **グラフ非表示＝FFT停止／泡アニメ停止／マスコット・サーバー・水彩を
  描かない**ため、実測で描画量が大きく減る（画像サイズ 291KB→204KB 等）。
- 描画・状態変更はすべて**メッセージスレッド**で完結。オーディオスレッド（processBlock）は
  パラメータをアトミックにライブ参照するだけで、モード切替の影響を受けない＝**リアルタイム安全**。

## 3. A/B プリセットのバグ修正
症状（報告）:「A↔B 往復で A が保存されない／B→A で音が変わらない／A>B が無反応」。
- 原因: A/B が独自の `apvts.replaceState` 経路を使い、かつ**デノイズ等の非APVTS状態を
  保存していなかった**。
- 修正: A/B スロットを**フル状態**（`getStateInformation`/`setStateInformation`＝ファイル保存と
  同じ実績経路）に統一。パラメータもデノイズも確実にスワップされる。
- A>B は**現在の状態を反対スロットへフルコピー**＋押した手応え（一瞬「✓」）＋方向表示
  （現在Aなら「A▶B」、Bなら「B▶A」）。
- 検証: シーン適用→B（既定へ）→A（復元）が**バイト単位で完全一致**（往復再現を確認）。

## 4. ゆるふわ強化（前回分）
- **ジュースノブ**: ノブをジュースグラス化（値＝果汁量、パネル別フルーツ、泡＋ゆらぎ）。
- **フォント Mochiy Pop One を埋め込み**（UI使用字のみサブセット, 5.16MB→約234KB, SIL OFL 1.1,
  ライセンス全文同梱）。パステル系モード（ゆるふわ／ゆるふわ軽量）で適用。
- **ぷにぐじを大きく・透過**で左下に配置（操作を邪魔しない主役）。
- **ジュースサーバー**の飾りを右下隅に追加（タンク・果汁・蛇口・さくらんぼ・ストロー）。

## 5. 十字スイッチのデザイン（文献調査に基づく）
セグメンテッドコントロール/トグルの原則（全選択肢を常時表示・相互排他・即時反映・十分な
タップ領域・明確なアクティブ表示・アイコンや色で区別）を踏まえて作り込み:
- **モード別のアクセント色**（標準=スレート/ゆるふわ=さくら/軽量=グレー/ライト=琥珀/
  ゆるふわ軽量=ミント）でセルを色分け。
- **モード別アイコン**（標準=ドット／ゆるふわ=ハート／軽量=ミニマル二本線／ライト=太陽／
  ゆるふわ軽量=ハート輪郭）。
- **アクティブセル**は色を満たし白アイコン、**ホバー**でセルが明るくなり**モード名ラベル**を
  表示（子の上に描く `paintOverChildren`）。背後に軽いトラックを敷き「一つの操作子」に見せる。

## 実装範囲
- 主に `Source/PluginEditor.h` / `Source/PluginEditor.cpp`（＋A/B修正でprocessorのフル状態経路を利用）。
- `Resources/`：`puniguji_kawaii.png`（マスコット）、`kawaii_font.ttf`＋`kawaii_font_LICENSE_OFL.txt`。
- バージョン 1.7.0（CMakeLists.txt / Installer.iss）。

## 検証（Linux / JUCE 8.0.12 / CMake+Ninja）
- Standalone をビルド：警告のみ・エラーなし。
- Xvfb で全5モードを十字クリックで切替・撮影し画素解析:
  - グラフ領域の彩度画素 標準/ゆるふわ/ライト≒3200〜4200 vs 軽量/ゆるふわ軽量≒748（**グラフ非表示**）。
  - 背景輝度 ダーク≒56／パステル≒234／ライト≒219。
  - 各モードでアクティブセルが正しいアクセント色で点灯。
  - 中央へ戻すと標準と完全一致（diff≒0.03）。
  - A/B 往復の状態復元がバイト一致。

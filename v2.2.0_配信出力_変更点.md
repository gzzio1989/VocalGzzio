# v2.2.0 — 配信出力（単体起動版の音を配信に乗せる）

## なぜ作ったか

単体起動版で歌うとき、モニターはヘッドホン（ASIO）で聴きたい。でも **ASIO の音は他のアプリから拾えません**。OBS などの配信ソフトに加工済みの声を送るには、どこかで「もう1本」音を出す必要がありました。

SYNCROOM Driver のような**専用の仮想オーディオドライバを自作する案は見送りました**。理由は次のとおりです。

- Windows のカーネルモードドライバになるため、**EV コードサイニング証明書**（年間およそ 3〜7万円）と **Microsoft パートナーセンターへの登録＋証明書によるアテステーション署名**が必須。署名が無いと Windows 10/11 の 64bit では読み込まれません。
- 不具合がブルースクリーンに直結し、Windows のアップデートのたびに壊れていないか確認が要る。個人開発で持ち続けるには重い。
- 効果に対して費用と手間が見合わない（同じことが下の方法で実現できる）。

そこで、**プラグイン側から「もう1つの出力先」へ同時に音を流す**方式にしました。これならドライバを書かずに、既存の仮想オーディオデバイス（VB-CABLE 等）を経由して配信に乗せられます。

---

## 使い方

1. 仮想オーディオデバイスを入れる（**VB-CABLE** が定番。寄付歓迎の無料ソフト／ https://vb-audio.com/Cable/ ）
2. VocalGzzio 単体起動版 → 右上「こだわり」→「エフェクト」タブ → 右下の **「配信出力」**
3. **配信先**で `CABLE Input (VB-Audio Virtual Cable)` を選び、**「配信に送る」**を押す
4. OBS で「音声入力キャプチャ」を追加し、デバイスに `CABLE Output (VB-Audio Virtual Cable)` を選ぶ

これで、**自分の声はヘッドホンで聴いたまま**、加工済みの声が配信に乗ります。設定は自動保存されるので、次回起動時は自動でつながります。

> **もっと簡単な方法（仮想デバイス不要）**
> メイン出力を ASIO ではなく「Windows Audio（WASAPI）」にしているなら、OBS の
> **「アプリケーション音声キャプチャ」**で `VocalGzzio.exe` を選ぶだけで拾えます。
> ASIO はこの方法では拾えないので、その場合に上の配信出力を使ってください。

---

## 技術メモ（クロックずれの吸収）

メインの出力デバイスと配信先デバイスは**別々の水晶で動く**ので、どちらも「48kHz」と名乗っていても実際には少しずれます。放っておくと片方のバッファが必ず溢れるか空になり、数分〜数十分ごとにプチッと鳴ります。

そこで、

- オーディオスレッドは**ロックフリーの FIFO に書くだけ**（確保・IO なし。本線の音には一切影響しない）
- 配信先のコールバック側で **FIFO のたまり具合**を見て、再生速度を **±0.4% の範囲**で常に微調整（可変レート補間 = Lagrange 補間）
- 目標のたまり具合は約 60ms。起動時は無音で埋めてから流し始めるので、出だしで途切れない

としています。サンプリングレートが違うデバイス（本線 48k・配信先 44.1k など）でもそのまま動きます。

### 実装の位置
- `Source/StreamOut.h`（新規）: FIFO＋可変レート補間＋デバイス管理
- `PluginProcessor`: `processBlock` の最後で `push()`。設定は状態XMLの `<STREAMOUT>` に保存
- `PluginEditor`: `StreamOutPanel`（CallOutBox）＋「配信出力」ボタン
- **単体起動版でのみ表示**します（プラグイン版は DAW が配線するため不要）
- 配信先は「機材の設定」なので、**A/B切替では持ち越し**ます（MIDI割当と同じ扱い）
- ASIO デバイスは配信先の一覧に出しません（メイン出力と取り合いになるため）

## 制限
- 配信先が詰まった場合、配信側の音が一瞬途切れることがあります（本線のモニター音は影響を受けません）。
- 配信までの遅延は約 60ms 増えます（OBS 側の同期オフセットで合わせられます）。

# v2.3.0 — ポップ／リップ ノイズ除去、そして Cubase が終了できない問題の修正

## ★ 重大な不具合の修正: Cubase 13 が終了できない

**症状**: インサートから VocalGzzio を外さないと Cubase を終了できない。

**原因は2つありました。どちらも「プラグインの寿命を超えて残るもの」です。**

### 原因1: 画面を開くたびに GitHub へ通信していた（主因）
更新確認のために、画面を開くたびに**切り離したスレッド**を起こして GitHub の API へ接続していました（最長4秒待ち）。そのスレッドが実行しているコードは VocalGzzio の DLL の中にあります。

通信の途中で DAW が終了してプラグインのモジュールを解放しようとすると、**Windows は「そのDLLのコードを実行中のスレッドが終わるまで」解放を待ちます**。ここで止まっていました。インサートから外すと終了できたのは、外した時点で待ち状態が解消されるためです。

**対策**: プラグイン版では通信しません（更新確認は単体起動版とホームページで行います）。単体起動版でも1回の起動につき1度だけ、待ち時間も2.5秒に短縮しました。

### 原因2: 埋め込みフォントを静的変数に入れっぱなしにしていた
ゆるかわ系テーマ用の書体を `static` に保持していたため、プラグインの全インスタンスが消えても参照が残り続けていました。DAW 終了時、**モジュール解放中（Windowsのローダーロック下）にフォントの解放処理が走る**形になり、これも停止・異常終了の原因になります。

**対策**: 最後の1つが閉じられた時点で必ず手放すようにしました（参照数で管理）。

### ついでに
配信出力で使うオーディオデバイス管理も、**単体起動版で実際に使うときだけ生成**するように変えました（プラグイン版では一度も触りません）。

> ご報告ありがとうございました。ホストを巻き込む種類の不具合なので、最優先で直しました。

---

## 新機能: ポップ／リップ ノイズ除去

**セクション1「きれいにする」に2つ追加**（初期値0% = 従来と完全に同じ音）。

### ポップ（破裂音）
「ぱ・ば行」で息がマイクにぶつかって出る**“ボフッ”という低い爆発音**だけを狙って抑えます。

低い歌声の基音と取り違えないよう、**(1) 突発的である** ことと **(2) 170Hz 以上の帯域（歌なら必ず倍音が出る）に対して低域が突出している** ことの両方を満たしたときだけ動きます。動作中は急峻なハイパス（190Hz / 24dB/oct）へ滑らかに寄り、収まると15msで戻ります。

ローカットを常時上げるのと違い、**歌の低音の太さは残ります**。

### リップ（口の粘着音）
フレーズの合間の**“ペチャ”“クチャ”という粘着音**を目立たなくします。3〜4kHz に一瞬だけ立つ鋭い音を検出し、その瞬間だけ高域を落とします（2.2kHz のローパスへ寄せる）。

**歌っている最中は絶対に動きません**（800Hz以下の帯域を見て「今は歌っている」と判定したら停止）。なのでサ行・タ行が鈍ることはありません。

### 検証（`tools/dsp_popclick.cpp`）
合成した信号で実測しました。

| 条件 | 結果 |
|---|---|
| 破裂音（60Hz の爆発音）単体 | **−7.6 dB** |
| 口の粘着音（3.5kHz の短い音）単体 | **−7.0 dB** |
| **伸ばした歌声（150Hz / 85Hz）** | **±0.0 dB**（一切触らない） |
| 女声の歌い出し60ms | −0.1 dB |
| 低い男声（85Hz）の歌い出し60ms | −3.2 dB（約48ms） |

**正直に書いておくべき制限**: VocalGzzio はゼロ遅延設計のため先読みができません。そのため**低い男声（基音85Hz付近）の歌い出しの一瞬だけ、わずかに反応することがあります**（上表）。影響が長引かないよう戻りを15msに詰めてあります。気になる場合はポップを50%程度に下げてください。市販の先読み型（数ms〜数十msの遅延を伴う）プラグインはこの制約がない代わりに遅延が出ます。

### 技術メモ
どちらも「**平行して走らせたフィルタへ、必要な瞬間だけ滑らかに寄せる**」方式です。フィルタ係数を作り直さないので**サンプル単位で追従**でき、遅延も CPU 負荷もほとんど増えません。
- 検出用の側鎖: ポップ = LP120Hz + HP170Hz（倍音側）、リップ = BP3.5kHz + LP800Hz（歌っているかの判定）
- 適用: ポップ = HP190Hz×2段へクロスフェード、リップ = LP2.2kHz×2段へクロスフェード

---

## UI
- セクション1が7ノブになったので、**左列を460px→520pxへ拡張**しました。セクション2・3のノブはむしろ大きくなり、右列（EQ／エフェクト）も従来どおり収まっています。

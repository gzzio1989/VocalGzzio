# GitHub Release用コメント (v2.0.1)

タグ: `v2.0.1` / タイトル: **v2.0.1 — ハモリ大改修（ハモだけ修正・9種選択・反行・エモート）**

以下をそのままリリース本文へ:

---

## ハモリを、作り直しました

### 修正
- **「ハモだけ」で原音が残って聴こえる問題を修正。** 原因は (1) 本人とほぼ同じ音程のユニゾン隊2声が出力に残っていた (2) Mixツマミが原音を混ぜ戻していた (3) 子音・息つぎでハモリ音程が一瞬0半音(=本人)へ落ちていた、の3点。ハモリ選択時は実際にハモる2声だけを出力し(+3dB補正)、ハモだけON中はMixの原音側を無効化、無声区間は直前の音程を保持します。プラグインの遅延報告は正確なので、録ったハモリはDAWの補正で原音トラックと揃います。
- **ハモリの選択肢がプルダウンに4つしか出ていなかった問題を修正。** 表示と実際の音がズレており、「上5度」で3度下が鳴る状態でした。9モード（ユニゾン/上3度/下3度/上6度/下6度/上5度/3度+5度/おまかせ/反行）を正しい順で表示します。
- **文字色の視認性を全モードで自動保証。** 実背景とのコントラスト比(4.5:1)を計算し、足りない文字色は自動補正。配信音量メーターの説明、「おまかせ設定」見出し等が読めるようになりました。シーンボタンの文字切れ(「弾き…」)も修正。
- **プルダウンの漢字フォント混在を修正。** 埋め込みフォントに不足していた195文字を追加(サブセット再生成スクリプト `tools/make_font_subset.py` 同梱)。

### 新機能
- **反行ハモリ**: メロディと逆方向へ動く対旋律。12キー×8スケール・245,760回の自動検査でスケール外0件・濁る音程0件。
- **エモート3機能**(初期値0%=音は従来と同一):
  - **息** — 小声のときだけ息の帯域を持ち上げる逆ディエッサー
  - **エモ** — ロングトーンで響きとひろがりがふわっと開く
  - **サビリフト** — サビを自動検出して空間系をまとめて持ち上げるオートアレンジ

### 互換性
- 既存プロジェクト/プリセットはそのまま読めます(`jn_harm` は末尾追加のみ、新パラメータは全て0%初期値)。
- 遅延はどの機能でも増えません。

### 検証
- 反行エンジン: 245,760リーディング(12キー×8スケール×ランダム旋律) → スケール外0 / 不協和音程0 / 反行・斜行率75.7%
- 既存ハモリ5種の9,180通り検査(v1.9.9)は引き続き全パス(度数テーブル変更なし)

---

**Full Changelog**: `v1.9.9...v2.0.1`

# VocalGzzio ビルド手順

このソースから **Windows の VST3 / Standalone**（および macOS の AU/VST3）を作る方法です。
中身は純粋な JUCE（8.0.12）なので、環境さえあればそのままビルドできます。

---

## 方法A：GitHub Actions でビルド（いちばん簡単・PC環境の準備が不要）

このリポジトリには CI 設定（`.github/workflows/build.yml`）が入っています。

1. ソース一式を GitHub のリポジトリに push（`main` か `master` ブランチ）。
2. GitHub の **Actions** タブを開くと、ビルドが自動で走ります（Windows と macOS 両方）。
3. 完了したら、その実行結果の下の **Artifacts** から
   - `VocalGzzio-windows`（`VocalGzzio.vst3` と `VocalGzzio.exe`）
   - `VocalGzzio-macos`（`.vst3` / `.component` / `.app`）
   をダウンロード。
4. リリースを作る場合は、タグ（例 `v2.9.0`）を付けて push すると、そのタグでもビルドされます。

> ※ Mac を持っていなくても、CI が Mac バイナリを作れます（未署名なので初回は右クリック→開く）。

---

## 方法B：自分のPCでビルド（Windows / ローカル）

### 必要なもの
- **Visual Studio 2022 以降**（「C++ によるデスクトップ開発」ワークロード）※ Build Tools だけでも可
- **CMake** 3.22 以上（https://cmake.org/download/ ／VS同梱のものでも可）
- **Git**（JUCE を自動取得するため。※ CMake が JUCE 8.0.12 を GitHub から取ってきます＝初回はネット接続が必要）

### 手順（**VS2026 の Developer Command Prompt** で、上から順に貼る）

```bat
cd C:\Users\silve\Downloads\VocalGzzio_GitHub_ready\VocalGzzio
```

★**バージョンを上げた回は、必ずここで build フォルダを消す**（下記「版が古いまま問題」参照）。
版が変わっていない普段のビルドでは、この1行は飛ばして構いません。

```bat
rmdir /s /q build
```
```bat
cmake -B build -DCMAKE_BUILD_TYPE=Release
```

構成が終わると `-- VocalGzzio: バージョン 2.9.0 で構成します` と出ます。
**ここの番号が、画面左上の表示と exe のプロパティの両方の出どころ**です。
ここが古ければ、この先を進めても古いままです。
```bat
cmake --build build --config Release --parallel 4
```
- 2行目：構成（初回は JUCE のダウンロードで数分かかります）。
  **バージョンを上げたときは必ずここから**。版番号はこの構成時にバイナリへ焼き込まれます。
- 3行目：ビルド

### 出来たものの版を確かめる（cmd から）
`Get-Item` は PowerShell の書き方なので、Developer Command Prompt では
`powershell -NoProfile -Command` を頭に付けます。

```bat
powershell -NoProfile -Command "(Get-Item 'build\VocalGzzio_artefacts\Release\Standalone\VocalGzzio.exe').VersionInfo.FileVersion"
```

### VST3 の manifest が消えているか確かめる
このプロジェクトは `VST3_AUTO_MANIFEST FALSE`（manifest を**同梱しない**）です。
ビルド後に `moduleinfo.json` が残っていると、ホストによってはスキャンから漏れます。

```bat
if exist "build\VocalGzzio_artefacts\Release\VST3\VocalGzzio.vst3\Contents\moduleinfo.json" (echo NG: manifest が残っています) else (echo OK: manifest なし)
```

### ★「入れ直したのに画面の版が古いまま」問題について（2026-08-05 判明）

**症状**: `CMakeLists.txt` は新しい版なのに、出来た exe のプロパティも画面左上も古い番号のまま。

**原因**: 版番号は `juceaide` が作る**生成ファイル**（版リソース `.rc` と コンパイル定義
`JucePlugin_VersionString`）に入ります。Visual Studio(MSBuild) は、この生成ファイルを
**日付だけ**で「作り直す必要なし」と判断することがあり、作り置きの `build` フォルダに対して
`cmake -B build` をやり直しても**古い番号が残り続けます**。
実例: CMakeLists が 2.9.0、その後にビルドした exe が `2.8.0` と申告していた。

**対策**: **版を上げた回は `rmdir /s /q build` してから構成する。** これだけです。
（フルビルドになるので数分かかりますが、確実です）

**確かめ方**: 下の2つが**両方**新しい番号になっていること。片方だけでは不十分です。

```bat
powershell -NoProfile -Command "(Get-Item 'build\VocalGzzio_artefacts\Release\Standalone\VocalGzzio.exe').VersionInfo.FileVersion"
```
```bat
findstr /c:"JucePlugin_VersionString" build\VocalGzzio.vcxproj
```
（前者＝exeのプロパティ、後者＝画面左上に描かれる文字列。出どころが別なので両方見ます）

いちばん確実なのは、**単体起動版を実際に起動して左上を見る**ことです。

```bat
build\VocalGzzio_artefacts\Release\Standalone\VocalGzzio.exe
```

### ビルド中に出る警告について
- `'pwsh.exe' は…認識されていません` … **無視して構いません**。JUCE 8.0.12 にも
  このリポジトリの CMakeLists にも `pwsh` は入っていない（どちらも grep 済み）ので、
  CMake の VS ジェネレータか VS2026 側の補助ステップです。成果物の生成には影響しません
  （`moduleinfo.json` の削除は CMake 自身のコマンドなので、pwsh が無くても実行されます）。
  気になる場合は PowerShell 7 を入れると消えます: `winget install --id Microsoft.PowerShell`
  出どころを特定したいときは: `findstr /s /i /c:"pwsh" build\*.vcxproj`
- `C4996 juce::Font` / `C4244` / `C4456` / `getenv` … JUCE 由来と既存コードの軽微な警告です。
  エラーではないので、そのまま進めて問題ありません。

### 出来上がり
```
build\VocalGzzio_artefacts\Release\VST3\VocalGzzio.vst3      ← これがVST3プラグイン
build\VocalGzzio_artefacts\Release\Standalone\VocalGzzio.exe ← 単体アプリ版
```

> ネットが使えない環境用に、JUCE をローカルに置いてビルドすることも可能です：
> `cmake -B build -DCMAKE_BUILD_TYPE=Release -DFETCHCONTENT_SOURCE_DIR_JUCE=（JUCE 8.0.12 のフォルダ）`

---

## VST3 の設置（Windows）

`VocalGzzio.vst3` を次のフォルダにコピー：
```
C:\Program Files\Common Files\VST3
```
その後、DAW／配信ソフト（Cubase・Reaper・OBS・SYNCROOM 等）でプラグインを再スキャンしてください。

---

## インストーラーを作る場合（任意・Inno Setup）

同梱の `Installer.iss` を **Inno Setup**（https://jrsoftware.org/isdl.php）で開いてコンパイルすると、
配布用のインストーラー `.exe` が作れます。
- 先に方法A/Bで `VocalGzzio.vst3` をビルドしておく必要があります。
- バージョンは `Installer.iss` の `MyAppVersion`（現在 2.9.0）で管理しています。
- コマンドラインからも作れます（Developer Command Prompt から。**先に Cubase を閉じる**）:

```bat
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" Installer.iss
```
```bat
installer_output\VocalGzzio_Setup_v2.9.0.exe
```

  `[InstallDelete]` が古い VST3 バンドルを丸ごと消してから入れるので、
  「新しい版を入れたのに画面の版表示が古いまま」を断てます。

---

## メモ
- 対応フォーマット：Windows = VST3 / Standalone、macOS = AU / VST3 / Standalone（CMakeが自動で切替）。
- C++17 / JUCE 8.0.12 / `JUCE_WEB_BROWSER=0` `JUCE_USE_CURL=0`（外部依存を減らす設定）。
- 埋め込みフォント（Mochiy Pop One サブセット）は SIL OFL 1.1。ライセンス全文は
  `Resources/kawaii_font_LICENSE_OFL.txt` に同梱済み（再配布時はこのファイルも一緒に）。

## ⚠ UIの文言に新しい漢字を足したら（v2.0.0〜）
`Resources/kawaii_font.ttf` はUIで使う文字だけのサブセットです。
Tooltips.h などに**新しい漢字**を足したら、次を実行してフォントを作り直してください
（やらないと、ゆるかわ/自然モードでその漢字だけ別フォントになります）:

```
pip install fonttools
python tools/make_font_subset.py （MochiyPopOne-Regular.ttf のパス）
```

フル版フォントは https://github.com/google/fonts/tree/main/ofl/mochiypopone から取得。

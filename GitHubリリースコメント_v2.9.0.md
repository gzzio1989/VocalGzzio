# GitHub リリース v2.9.0 用の本文

タイトル: `v2.9.0 — セッションモード（追加遅延を0サンプルに固定）`

---

オンラインセッション（SYNCROOM など）で使うための回です。
**「低遅延」ボタンを廃止**して、**追加遅延を0サンプルに固定する「セッション」スイッチ**に
置き換えました。あわせて、調べている途中で見つかった**遅延の申告バグを1件**直しています。

## なぜ「低遅延」を捨てたのか

旧ボタンはピッチ系の遅延を **768 → 384 サンプル（17.4 → 8.7ms）** に半分にするものでした。
分析の窓を縮めるので、低い声はわずかにざらつきます。

ところがオンラインセッションで快適とされる **往復30ms** は、**ネットとパソコンを通した
合計の実測値**です。自分のパソコンの持ち分はその半分も残りません。オーディオ
インターフェースのAD/DAとバッファだけで7〜8ms使うので、**プラグインに許される
追加遅延は実質ゼロ**です。

つまり 8.7ms は「半分になった」のではなく「**まだ足りない**」でした。音質を犠牲にして、
足りない答えを出していたことになります。**「減らす」をやめて「足さない」を保証する**
構えに変えました。

## セッションモード

画面上部の右端に、**「追加遅延」バッジ**と**「セッション」スイッチ**を置きました。
かんたん／こだわりのどちらでも同じ位置に出ます。

- ONの間は、遅延をふやす3つ（**ピッチ補正・ボイス変換・ハモリ／5人ユニゾン**）を素通しに
  します。スイッチの値そのものは変えないので、**セッションを抜ければ元どおり**です
- 止めている間、その3つのツマミは**灰色**になります。v2.8.0で直した「見えないのに
  効いている」の逆、**「見えているのに効いていない」**を起こさないためです
- バッジは**いま何ms足しているか**を常に出します。0なら緑。再生中にピッチ補正をONに
  すればその場で **+17.4 ms** に変わります（申告値ではなく、いま実際に遅れている値です）

## 直したバグ: ハモリだけONにすると、歌だけ17.4ms走っていた

**ハモリ（5人ユニゾン）だけをON**にすると、プラグインはホストへ「768サンプル遅れます」と
申告していました。ところが実測すると**主メロのピークは0サンプル**、まったく遅れていません。

ハモリは主メロの**横に**声を足す並列処理なので、主メロ自体は遅れません。申告だけが
17.4ms嘘をついていました。実害は2つです。

1. **DAWがその嘘を信じてトラックを17.4ms前へ引っ張る** → 歌だけ走る
2. **Mixの原音そろえも同じ値を見ていた** → 原音だけ17.4ms遅れて混ざり、v2.8.0で直した
   はずの**コムフィルタ（スカスカ）がハモリのときだけ再発**していた

主メロが実際に遅れるのはボイス変換／ピッチ補正のときだけなので、申告の条件から
ハモリを外しました。

## 確認したこと

`tools/bench_lat.cpp`（新規）で、機能ごとに1本ずつ測りました。
44.1kHz / 124サンプル（締切2.81ms）。

| 場面 | 申告 | 実測 |
|---|---|---|
| 出荷状態 | 0 | **0** |
| セッション構成（ジー音/ことば/圧縮/艶/自動音量/軽リバーブ） | 0 | **0** |
| 空間ぜんぶ（やまびこ/コーラス/ひろがり/かさね/シマー/リング） | 0 | **0** |
| **ハモリ／5人ユニゾンのみ** | **0**（旧768＝バグ） | **0** |
| ハモリ＋ピッチ補正 | 768 | **768** |
| ピッチ補正80 | 768 | **768** |
| ボイス変換のみ（+3半音） | 768 | 943（±1ホップ内＝正常） |
| **★セッションON ＋ ピッチ補正80** | **0** | **0** |
| **★セッションON ＋ 全部盛り** | **0** | **0** |

セッション構成の負荷は **平均 5.5% / 99%点 7.5% / 最悪 11.0%**（締切=100%）。
実起動でも、バッジとスイッチが何にも重なっていないこと、ONで該当ツマミが灰色になること、
ONのときのボタン文字が読めることまで目で確認しています。

---

**セッションモードをONにしなければ、設定を変えていない限り音は変わりません。**
ハモリだけを使っていた方は、Mixの原音そろえが直るぶん**スカスカが解消します**（直った方向）。
旧「低遅延」をONにして保存していたプロジェクトは、通常の窓（768サンプル）に戻ります。
セッションで使っていた方は、かわりに**セッションスイッチ**をどうぞ（0msになります）。

動作環境: Windows 10 / 11 (64bit)、VST3 または スタンドアロン
未署名のため、初回起動時に SmartScreen の警告が出ることがあります
（「詳細情報」→「実行」で進めてください）。

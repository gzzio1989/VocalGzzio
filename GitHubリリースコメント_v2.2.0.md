# GitHub Release用コメント (v2.2.0)

タグ: `v2.2.0` / タイトル: **v2.2.0 — 配信出力＋MIDIスイッチ＋ハモリ大改修**

以下をそのままリリース本文へ:

---

## 単体起動版の声を、そのまま配信へ

### 新機能: 配信出力（単体起動版）
仕上がった声を、いま聴いているヘッドホンとは**別の出力先へ同時に**流せるようになりました。

**使い方**
1. 仮想オーディオデバイスを入れる（**VB-CABLE** が定番／ https://vb-audio.com/Cable/ ）
2. 「こだわり」→「エフェクト」タブ → 右下の **「配信出力」**
3. 配信先で `CABLE Input (VB-Audio Virtual Cable)` を選び、**「配信に送る」**
4. OBS で「音声入力キャプチャ」を追加し、`CABLE Output` を選ぶ

自分の声はヘッドホンで聴いたまま、加工済みの声が配信に乗ります。設定は自動保存され、次回起動時に自動でつながります。

> **仮想デバイスなしで済む場合**: メイン出力が ASIO ではなく Windows Audio（WASAPI）なら、OBS の「アプリケーション音声キャプチャ」で `VocalGzzio.exe` を選ぶだけで拾えます。ASIO はこの方法では拾えないため、その場合に配信出力を使ってください。

**なぜ専用ドライバを作らなかったか**: SYNCROOM Driver のような仮想オーディオドライバは Windows のカーネルモードドライバになり、EV コードサイニング証明書とパートナーセンターでのアテステーション署名が必須、かつ不具合がブルースクリーンに直結します。個人開発で持ち続けるには重すぎるため、プラグイン側から二重出力する方式にしました。

**クロックずれの吸収**: 2つの出力デバイスは別々の時計で動くため、そのままでは数分ごとにプチッと鳴ります。ロックフリー FIFO のたまり具合を見て再生速度を ±0.4% の範囲で常に微調整し、そのズレを吸収しています（サンプリングレートが違うデバイス同士でも動作）。オーディオスレッド側は FIFO に書くだけなので、本線のモニター音には一切影響しません。

### MIDIスイッチ（v2.1.0）
- フットスイッチ／パッドのMIDI（ノート・CC）に **12種**を割り当て: A/B切替 / ひびきON・OFF / ひびきの種類 送り / 5人ユニゾン / **ハモだけ** / ハモリの種類 送り / ピッチ補正 / ボイス変換 / やまびこ / キー半音上げ・下げ。**8枠**。
- 設定はエフェクトタブの **「MIDI設定」**。やることを選んで「学習」を押し、スイッチを踏むだけ（チャンネル不問、CCは値64以上を押下として判定）。
- 割り当てはプリセット／自動保存に保存され、**A/Bを切り替えても消えません**。
- A/B比較の中身をプラグイン本体が持つ方式に変更（画面を閉じていてもMIDIから切替可能）。
- **VST3にMIDI入力を追加**（DAWでのルーティング設定が必要。単体起動版は Options → Audio/MIDI Settings で機器を有効に）。

### ハモリ大改修（v2.0.0）
- **「ハモだけ」で原音が残って聴こえる問題を修正。** 原因は (1) 本人とほぼ同じ音程のユニゾン隊2声が出力に残っていた (2) Mixツマミが原音を混ぜ戻していた (3) 子音・息つぎでハモリ音程が一瞬0半音へ落ちていた、の3点。
- **ハモリの選択肢がプルダウンに4つしか出ていなかった問題を修正**（「上5度」で3度下が鳴る状態でした）。9モードを正しい順で表示。
- **反行ハモリ**（新規）: メロディと逆方向へ動く対旋律。245,760回の自動検査でスケール外0件・濁る音程0件。
- **エモート3機能**（初期値0%=音は従来と同一）: 息 / エモ / サビリフト。
- プルダウンの漢字フォント混在を修正（不足195文字を追加）。

### 見た目の修正（v2.0.1）
- ヘッダーのバージョン表記とサブタイトルを拡大。
- 淡いテーマでプルダウンの案内文字が読めなかった問題を修正（明るいテーマでは濃い箱＋明るい文字に反転）。
- 文字色は実背景とのコントラスト比を計算して自動補正。シーンボタンの文字切れも修正。

### 互換性
- 既存プロジェクト／プリセットはそのまま読めます。遅延はどの機能でも増えません（配信出力の経路のみ約60ms、モニターには影響なし）。
- ⚠ VST3にMIDI入力バスが増えたため、DAWによっては初回にプラグインの再スキャンが走ります。

---

**Full Changelog**: `v1.9.9...v2.2.0`

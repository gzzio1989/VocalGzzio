# GitHub Release用コメント (v2.3.0)

タグ: `v2.3.0` / タイトル: **v2.3.0 — ポップ/リップ除去＋Cubase終了バグ修正（配信出力・MIDIスイッチ・ハモリ大改修を含む）**

以下をそのままリリース本文へ:

---

## ★ Cubase 13 が終了できない問題を修正しました

**症状**: インサートから VocalGzzio を外さないと Cubase を終了できない。

**原因は2つ**、どちらも「プラグインの寿命を超えて残るもの」でした。

1. **画面を開くたびに GitHub へ通信していた（主因）**。更新確認のために切り離したスレッドを起こしており、そのコードは VocalGzzio の DLL 内で動きます。通信中（最長4秒）に DAW がモジュールを解放しようとすると、Windows は「そのDLLを実行中のスレッドが終わるまで」解放を待つため、そこで停止していました。→ **プラグイン版では通信しません**（更新確認は単体起動版とホームページで）。
2. **埋め込みフォントを静的変数に保持し続けていた**。全インスタンスが消えても参照が残り、DAW 終了時のモジュール解放中にフォントの解放処理が走っていました。→ **最後の1つが閉じられた時点で必ず手放します**。

あわせて、配信出力のオーディオデバイス管理も単体起動版で実際に使うときだけ生成するようにしました。

## 新機能: ポップ／リップ ノイズ除去
セクション1「きれいにする」に2つ追加（初期値0% = 従来と完全に同じ音）。

- **ポップ（破裂音）**: 「ぱ・ば行」で息がマイクにぶつかる“ボフッ”という低い音だけを狙って抑えます。**(1)突発的である** ことと **(2)歌なら必ず出る倍音（170Hz以上）に対して低域が突出している** ことの両方を条件にしているので、低い歌声の基音と取り違えません。ローカットを常時上げるのと違い、歌の低音の太さは残ります。
- **リップ（口の粘着音）**: フレーズの合間の“ペチャ”を目立たなくします。**歌っている最中は絶対に動かない**ので、サ行・タ行が鈍ることはありません。

**実測（`tools/dsp_popclick.cpp`）**: 破裂音 **−7.6dB** / 粘着音 **−7.0dB** / **伸ばした歌声 ±0.0dB（一切触らない）** / 女声の歌い出し −0.1dB。
ゼロ遅延設計で先読みができないため、**低い男声（基音85Hz付近）の歌い出しの一瞬（約48ms）だけ −3.2dB 程度反応する**ことがあります。気になる場合はポップを50%程度に下げてください。

**技術**: 「平行して走らせたフィルタへ、必要な瞬間だけ滑らかに寄せる」方式。係数を作り直さないのでサンプル単位で追従でき、遅延も CPU 負荷もほとんど増えません。

## 配信出力（v2.2.0）
仕上がった声を、いま聴いているヘッドホンとは**別の出力先へ同時に**流せます。VB-CABLE 等の仮想オーディオデバイスを配信先に選び、OBS でその出力をキャプチャすれば、自分の声はヘッドホンで聴いたまま配信に乗ります。2つのデバイスのクロックずれは再生速度を±0.4%で微調整して吸収しています。エフェクトタブの「配信出力」から（単体起動版のみ）。

## MIDIスイッチ（v2.1.0）
フットスイッチ／パッドのMIDI（ノート・CC）に **12種**を割り当て: A/B切替 / ひびきON・OFF / ひびきの種類送り / 5人ユニゾン / **ハモだけ** / ハモリの種類送り / ピッチ補正 / ボイス変換 / やまびこ / キー半音上げ・下げ。**8枠**。「MIDI設定」で「学習」を押してスイッチを踏むだけ。**VST3にMIDI入力を追加**しました。

## ハモリ大改修（v2.0.0）
- **「ハモだけ」で原音が残って聴こえる問題を修正**（ユニゾン隊2声の残存 / Mixの原音混ぜ戻し / 無声区間で音程が0に落ちる、の3点）。
- **ハモリの選択肢が4つしか出ていなかった問題を修正**（「上5度」で3度下が鳴る状態でした）。9モードを正しい順で表示。
- **反行ハモリ**: メロディと逆方向へ動く対旋律。245,760回の検査でスケール外0件・濁る音程0件。
- **エモート3機能**: 息 / エモ / サビリフト。
- プルダウンの漢字フォント混在、文字コントラストも修正。

## 互換性
- 既存プロジェクト／プリセットはそのまま読めます。新パラメータは全て0%初期値。
- ⚠ VST3にMIDI入力バスが増えたため、DAWによっては初回にプラグインの再スキャンが走ります。

---

**Full Changelog**: `v1.9.9...v2.3.0`

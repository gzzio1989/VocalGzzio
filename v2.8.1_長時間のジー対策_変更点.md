# VocalGzzio v2.8.1 変更点 — 長時間の「ジー」への対策

前バージョン: v2.8.0（公開済み）。音の出る機能は増減なし・設定もそのままです。

## ご報告「長い時間使うとジーというノイズが走る」の調査結果

**最有力の原因は、v2.8.0 で直した「音声処理の中の毎回のメモリ確保」です。**
124サンプルのバッファでは処理の締め切りが2.8msしかなく、v2.6.1まではそこに
1.3msごとに数十回のmalloc/freeが走っていました。確保にたまに時間がかかると
ブロックが落ち、**連続するとブロックの周期(約355Hz)の「ジー」に聞こえます**。
ヒープはセッションが長いほど荒れていくので「長時間で出る」とも辻褄が合います。
→ まず **v2.8.0 で長時間使って再発するか**をご確認ください。

## v2.8.1 で足したもの

1. **長時間シミュレーションの新設**（`tools/dsp_longrun.cpp`）
   時間とともに悪くなり得る部品へ「圧縮した長時間」を流して実測しました。

   | 部品 | 試験 | 結果 |
   |---|---|---|
   | ジー音（ハム除去） | 歌＋本物のハムを**20分** | 息継ぎの残りカス 序盤-63.4dB → 終盤**-66.1dB**（悪化なし） |
   | ジー音の学習重み | 20分間の本来のハムとの誤差 | 最大**0.00099**（声を掴んで溜め込まない） |
   | ボイス変換 | **8分**連続 +2半音 | 濁り 序盤-0.8dB → 終盤-0.7dB（位相は壊れない） |
   | 内部発振器 | **6時間ぶん** | 振幅ずれ最大 **3.6e-7** |

   ＝**音の処理そのものは長時間でも劣化しない**ことを数値で確認しました。

2. **ジー音の自己回復（保険）** … 内部状態が万一壊れると壊れた値が音に混ざり
   続ける構造だったので、0.1秒ごとに検査して壊れていたら自動で作り直します。

3. **インストーラの掃除を強化** … 上書きインストール時に古いVST3バンドルを
   丸ごと消してから入れるようにしました（新旧ファイルが混ざるのを防ぐ）。

## 「画面の左上が v2.6.1 のまま」について

表示は `JucePlugin_VersionString`（ビルド時にCMakeの版番号から自動生成）を
そのまま描いているので、**v2.6.1と表示される＝Cubaseが読み込んでいるVST3が
本当にv2.6.1のバイナリ**ということです。確認は次の順で:

1. Cubase の **プラグインマネージャーで VocalGzzio のパス**を見る
2. `C:\Program Files\Common Files\VST3\VocalGzzio.vst3` の日付を見る
3. **v2.8.0（またはこのv2.8.1）のインストーラをもう一度実行**して、Cubaseを再起動

古い場所への手動コピーが残っていると、新しい版を入れてもそちらが読まれます。
v2.8.1を入れれば左上が「v2.8.1」になるので、切り分けが一発で付きます。

## もし v2.8.1 でもジーが出たら

①ジー音つまみが**0%でも**出るか ②プラグインを**挿し直す**と直るか
③**Cubaseを再起動**すると直るか — の3点をお知らせください。原因がさらに絞れます。

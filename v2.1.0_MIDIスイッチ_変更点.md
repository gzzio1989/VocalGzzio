# v2.1.0 — MIDIスイッチ（足元で切り替える）

## 新機能: MIDIスイッチ

### 何ができるか
フットスイッチやパッドのMIDI（ノート／CC）に、下の**12種**を割り当てて手を使わずに切り替えられます。

| 割り当てられる操作 |
|---|
| A/B切替 / ひびき ON・OFF / ひびきの種類 送り / 5人ユニゾン ON・OFF |
| **ハモだけ ON・OFF** / ハモリの種類 送り / ピッチ補正 ON・OFF / ボイス変換 ON・OFF |
| やまびこ ON・OFF / キー半音上げ / キー半音下げ |

**枠は8個**。ライブなら「Aメロ用A / サビ用B」をフットスイッチ1つで行き来する、配信なら「ハモだけ」を足で入り切りする、といった使い方ができます。

### 設定のしかた
1. エフェクトタブ（画面右上「こだわり」→「エフェクト」）の右下 **「MIDI設定」** を押す
2. 左のプルダウンで**やること**を選ぶ
3. **「学習」**を押す（黄色く「待機中…」になる）
4. 割り当てたい**スイッチを踏む／押す** → その場で登録完了

- MIDIチャンネルは問いません（どのチャンネルでも反応します）。
- CCは**値64以上を「踏んだ」**、64未満を「離した」として扱います。モーメンタリ／ラッチどちらのフットスイッチでも動きます。
- 押した**瞬間だけ**発火します（踏みっぱなしで連打にはなりません）。
- 「解除」で割り当てを消せます。

### 保存とA/Bの扱い
- 割り当ては**プリセット・プロジェクト・自動保存に一緒に保存**されます。
- **A/Bを切り替えても割り当ては消えません**（機材の設定であって音の設定ではないため、意図的に持ち越しています）。

### MIDIの届け方
- **VST3にMIDI入力を追加しました**。DAW側でこのプラグインへMIDIを送る設定にしてください（トラックのMIDI出力先に指定する、等。ホストによって呼び方が違います）。
- **単体起動版**は Options → Audio/MIDI Settings で使うMIDI機器にチェックを入れてください。
- ⚠ MIDI入力バスが増えたので、DAWによっては初回にプラグインの再スキャンが走ります。

### 内部の作り（音を止めないための設計）
音声処理の中では**MIDIを見てフラグを立てるだけ**にして、実際の切替（A/Bの状態入れ替えやパラメータ変更）はメッセージスレッドで行っています。オーディオコールバック内でメモリ確保やホスト通知を行わないので、切り替えても音が途切れません。
あわせて **A/B比較の中身をプラグイン本体が持つ方式**に変更しました（従来は画面側が保持）。これで画面を閉じていてもMIDIから切り替えられます。

---

## 見た目の修正（v2.0.1分を含む）
- **ヘッダーのバージョン表記とサブタイトルを拡大**（10〜11px → 12.5px、バージョンは太字）。小さすぎて読めない状態を解消。
- **淡いテーマでプルダウンの案内文字が読めない問題を修正**。JUCEは未選択時の案内文字を「文字色の50%透過」で描くため、淡い箱の上では原理的に読めません。明るいテーマでは**プルダウンだけ濃い箱＋明るい文字に反転**して、コントラスト比4.5:1を確保しました。
- 文字色は実背景とのコントラスト比を計算して自動補正（全テーマ）。
- ヘッダーのシーンボタン（弾き語り／トーク／Band）の文字切れを修正。
- MIDI設定パネルもテーマの地色で描画（別ウィンドウなので、放っておくとJUCE既定のダーク配色で開いてしまう）。

## 技術メモ
- 新規: `VocalGzzioProcessor::processMidiSwitches()`（オーディオスレッド／判定のみ）、`handleAsyncUpdate()`（メッセージスレッド／実行）、`abSwitch()` / `abCopyToOther()`。
- 状態XMLに `<MIDIMAP>` セクションを追加（8スロット×act/type/num/ch）。既存の保存データは影響を受けません（無ければ未割当のまま）。
- `CMakeLists.txt`: `NEEDS_MIDI_INPUT TRUE` に変更、`acceptsMidi()` を true に。
- エディタ: `MidiMapPanel`（CallOutBox内）＋「MIDI設定」ボタン（エフェクトタブ・キャラ声行の右端）。

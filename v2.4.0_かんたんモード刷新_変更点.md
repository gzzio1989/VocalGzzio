# v2.4.0 かんたんモードの作り直し（2026-07-27 リリース済み）

## かんたんモードの作り直し

## 何が変わったか

### 1. 「かんたん／こだわり」ボタンを廃止しました
以前は右上に1つのボタンがあり、押すたびに文字が「かんたん」⇄「こだわり」と入れ替わる作りでした。これは**押すまでいまどちらのモードなのか分からない**という、トグルUIの典型的な失敗です（そのボタンの文字は現在の状態なのか、押した後になる状態なのか、見た目からは判別できません）。

これをやめて、ヘッダーに**大きな「かんたんモード」のON/OFFスイッチ**を1つ置きました。緑に光って丸が右にあればON、灰色で丸が左にあればOFF。形で状態が分かります。

### 2. かんたんモードが、名前どおり簡単になりました
旧「スタンダード」は、隠していたのがエフェクトタブ・音域ツール・テンポフィットだけで、**左のツマミ17個も右のEQ列もそのまま出ていました**。名前に対して中身が簡単ではありませんでした。

v2.4.0 のかんたんモードは、画面を横いっぱいに使って**よく使う7つのツマミだけ**を大きく表示します。

| ツマミ | 何をするか |
|---|---|
| ノイズ除去 | 環境音・サーというノイズを下げる |
| こもり | もわっとした低中域を抜く |
| キンキン | 耳に刺さる高域を抑える |
| ならし圧縮 | 音量の波をならす |
| サ行おさえ | 歯擦音を抑える |
| ひびき | リバーブの量 |
| エフェクト量 | 全体のかかり具合 |

右のEQ・エフェクト列はまるごと隠れます。パネルも1枚だけになり、番号（1〜4）も出しません。おまかせ設定とチューナーはそのまま残るので、**「おまかせを押す → 気になるところを1つ触る」**で完結します。

### 3. うた自動の10秒ワンボタン化（グッジオさん指定）
かんたんモードでは「うた自動」を画面中央に大きく置き（340×56px・文字20px、`"fontH"`プロパティで`getTextButtonFont`を上書き）、**1回押すだけで LEARN＋うた自動を10秒で終わらせる**。

内訳: 0〜0.5秒=間（押した直後の物音を拾わないための猶予）→ 0.5〜2.0秒=ノイズ学習（`requestDenoiseLearn()`、中身は1.5秒でLEARNと同一）→ 2.0〜10.0秒=`requestAutoSetup(1)`（8秒のうた解析）。

実装はエディタの20Hzタイマーで進める小さな状態機械（`easyComboPhase` 0=待機/1=しずかに/2=解析中、`easyComboTick`）。**音声スレッドには何も足していない**（既存の2つの仕組みを時間差で呼ぶだけ）。フェーズ1の間はボタンが「しずかにしてね… あと N秒」、フェーズ2は従来の「解析中… あと N%」。カウントダウン中にこだわりへ切り替えたら中断（解析まで進んでいたら続行）。こだわりモードのうた自動は従来どおり8秒単独。

### 3. テーマを一列に並べ直しました
＊型グリッド＋中央Nという配置をやめ、**ふつう／ゆるふわ／自然／ブランド／色覚／軽量**の6つを素直な一列にしました。中央の「N」は「ふつう」として並びに入っています。

**オーロラは廃止**し、その席をかんたんモードのスイッチに譲りました。保存データでオーロラを選んでいた場合は、ゆるふわで開きます（黒い画面にならないようにするため）。

---

## 実装メモ

### 変更したファイル
- `Source/PluginEditor.h` — `modeButton` を削除。`kThemeOfChip[6]` / `kChipH` / `kSwitchW` / `kSwitchH` を追加。`crossHit()` のコメントを更新。
- `Source/PluginEditor.cpp` — ヘッダー配置、`crossHit()`、`drawThemeCross()`、`mouseDown()`、`applyModeVisibility()`、`updateTabVisibility()`、`setThemeMode()`、`paint()`、`resized()`。
- `Source/Tooltips.h` — `mode_easy_switch()` / `theme_plain()` / `easy_panel()` / `easy_hint()` を追加。
- `CMakeLists.txt` / `Installer.iss` — 2.3.0 → 2.4.0。
- `docs/index.html` — バージョン表記と更新履歴。

### 気をつけた点
- **`crossHit()` の座標**: 下段のスイッチ幅を `kSwitchW = 244` に固定しています。ヘッダー右下は「文字／大きさ」スライダーの席（`W - pad - 112` 以降）なので、そこへ食い込まないための上限です。テーマチップを増やすときは上段の 6 等分だけを触ってください。
- **`setThemeMode()` の後方互換**: 旧データの `4`（オーロラ）と `6`（旧ステージ）は `1`（ゆるふわ）へ寄せています。ここを外すと、オーロラを保存していた人が未定義パレットで開いてしまいます。
- **`drawPanel()` に空判定を追加**: かんたんモードでは `dynArea` / `toneArea` / `spaceArea` / `seqArea` を空の矩形にしています。空なら描かない、という1行を入れてあります。
- **`paint()` の分岐**: パネル描画をまるごと `if (! advancedMode) { … } else { … }` で囲みました。`drawJuiceServer()` とゆるふわの大きなマスコットは**両モード共通**なので、`else` の外に残してあります（早期 return にすると消えます）。
- **ランプの後始末**: 隠したツマミのランプ（`lampGate` / `lampDbl` / `lampDly`）も一緒に消しています。残すと宙に浮いた赤い点になります。

### 文字とバランスの作り直し（2回目の修正）
最初の版は「ツマミだけ大きくして、文字とボタンは元のまま」だったので、全体のバランスが崩れていました。作り直した点は4つです。

**`rowScale`（新規メンバー）** — かんたんモードで `1.55` にして `placeRow()` に渡します。パネル見出しの高さ、ツマミ名の箱、数値の箱、テキストボックス幅をまとめて拡大します。**箱だけ広げても字は大きくなりません**（`drawFittedText` が箱に合わせて縮めるため）。なので Label の `"fontH"` プロパティも同時に設定しています（`GzzioLnF::getLabelFont()` がこれを読みます）。数値表示は Slider が内部で作る Label なので、`slider.getChildren()` を走査して同じプロパティを立てています。

**`heroBig`（新規メンバー）** — 「おまかせ設定／トーク自動／うた自動」の帯。ツマミだけ大きくしてボタンが小さいままだと、いちばん先に押してほしいボタンが一番目立たない、という逆転が起きます。かんたんモードでは帯を 42→68px、ボタンを 128×28→210×48、見出しを 15.5→20px にしました。

**`wideGlass`（Slider プロパティ）** — `drawJuiceKnob()` はグラス幅を「セル幅 × 0.62」で描きます。7本を横いっぱいに並べるとセル幅が約190pxになり、グラスは118px幅のまま縦だけ伸びて**細長い試験管**に見えていました。`wideGlass` が立っているときは係数を **0.90** にして、セル幅いっぱいに近づけます。

**縦の積み方** — 最初は上下中央に寄せていましたが、おまかせ設定とチューナーの間に用途のない空白ができて間延びしました。上から素直に積み、余りは下（背景とマスコットが出る帯）へ逃がします。

```
top    = heroArea.getBottom() + 8      // ヒーロー帯が高くなったぶん追従させる
tunerH = 195
panelH = min(390, 使える高さ - tunerH - 14)
```

`top` を 128 の直値から `heroArea.getBottom() + 8` に変えてあります。ヒーロー帯の高さがモードで変わるようになったので、直値のままだとかんたんモードで重なります。

---

## 検証したこと
- Linux フルビルド（Release、エラー0）。
- Xvfb 上で単体起動し、**かんたんモードON／OFF の両方でスクリーンショットを取得**。
  - ON: おまかせ設定 → チューナー → 7ツマミの1枚パネル。EQ列は完全に消えている。音程グラフの ♯高い／♭低い／ぴったり が読める。
  - OFF: 従来どおり全パネル（1きれいにする／2音量／3音色／4ひろがり＋EQ／エフェクト）が復帰。
  - スイッチの見た目が ON=緑・丸が右／OFF=灰・丸が左 で切り替わることを確認。
- `docs/index.html` を実描画してJSエラー0、更新履歴に v2.4.0 が先頭で入り、v2.3.0 以下が正しく続くことを確認。

## このバージョンについて
2026-07-27 リリース。新ツマミ群（なめらか・マイク音量・音量キープ・10秒おまかせ）は **v2.5.0** 側。

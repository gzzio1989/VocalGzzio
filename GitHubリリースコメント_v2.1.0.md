# GitHub Release用コメント (v2.1.0)

タグ: `v2.1.0` / タイトル: **v2.1.0 — MIDIスイッチ＋ハモリ大改修（ハモだけ修正・9種選択・反行・エモート）**

以下をそのままリリース本文へ:

---

## 足元で切り替えられるようになりました

### 新機能: MIDIスイッチ
- フットスイッチ／パッドのMIDI（ノート・CC）に、次の**12種**の操作を割り当てられます:
  A/B切替 / ひびき ON・OFF / ひびきの種類 送り / 5人ユニゾン ON・OFF / **ハモだけ ON・OFF** / ハモリの種類 送り / ピッチ補正 ON・OFF / ボイス変換 ON・OFF / やまびこ ON・OFF / キー半音上げ / キー半音下げ。
- 設定はエフェクトタブの **「MIDI設定」**。8枠あり、やることを選んで「学習」を押し、割り当てたいスイッチを踏むだけ（MIDIチャンネル不問。CCは値64以上を押下として扱います）。
- 割り当てはプリセット／自動保存に一緒に保存され、**A/Bを切り替えても消えません**（機材の設定なので音とは別扱い）。
- **A/B比較の中身をプラグイン本体が持つ方式**に変更。画面を閉じていてもMIDIから切り替えられます。
- **VST3にMIDI入力を追加**しました。DAWではこのプラグインへMIDIを送る設定を、単体起動版では Options → Audio/MIDI Settings でMIDI機器を有効にしてください。
- 切替の処理はメッセージスレッドで行うので、オーディオが途切れません（音声処理側はフラグを立てるだけ）。

### 見た目の修正
- ヘッダーのバージョン表記とサブタイトルを拡大（10〜11px → 12.5px、バージョンは太字）。
- 淡いテーマでプルダウンの案内文字（「声質を選択…」など）が読めなかった問題を修正。明るいテーマでは**濃い箱＋明るい文字**に反転します（JUCEは案内文字を50%透過で描くため、淡い箱では原理的に読めませんでした）。
- 文字色は実際の背景とのコントラスト比を計算して自動補正（全テーマ）。
- ヘッダーのシーンボタンの文字切れを修正。

### ハモリ大改修（v2.0.0の内容・ここが本体）
- **「ハモだけ」で原音が残って聴こえる問題を修正。** 原因は (1) 本人とほぼ同じ音程のユニゾン隊2声が出力に残っていた (2) Mixツマミが原音を混ぜ戻していた (3) 子音・息つぎでハモリ音程が一瞬0半音(=本人)へ落ちていた、の3点。ハモリ選択時は実際にハモる2声だけを出力し(+3dB補正)、ハモだけON中はMixの原音側を無効化、無声区間は直前の音程を保持します。
- **ハモリの選択肢がプルダウンに4つしか出ていなかった問題を修正。** 表示と実際の音がズレており、「上5度」で3度下が鳴る状態でした。9モード（ユニゾン/上3度/下3度/上6度/下6度/上5度/3度+5度/おまかせ/反行）を正しい順で表示します。
- **反行ハモリ**（新規）: メロディと逆方向へ動く対旋律。12キー×8スケール・245,760回の自動検査でスケール外0件・濁る音程0件。
- **エモート3機能**（初期値0%=音は従来と同一）: **息**（小声のときだけ息の帯域を持ち上げる逆ディエッサー）/ **エモ**（ロングトーンで響きとひろがりがふわっと開く）/ **サビリフト**（サビを自動検出して空間系をまとめて持ち上げる）。
- **プルダウンの漢字フォント混在を修正**。埋め込みフォントに不足していた195文字を追加（再生成スクリプト `tools/make_font_subset.py` 同梱）。

### 互換性
- 既存プロジェクト／プリセットはそのまま読めます（`jn_harm` は末尾追加のみ、新パラメータは全て0%初期値）。
- 遅延はどの機能でも増えません。
- ⚠ VST3にMIDI入力バスが増えたため、DAWによっては初回にプラグインの再スキャンが走ります。

### 検証
- 反行エンジン: 245,760リーディング（12キー×8スケール×ランダム旋律） → スケール外0 / 不協和音程0 / 反行・斜行率75.7%
- Linux（JUCE 8.0.12）でフルビルド＋実起動し、全テーマ・エフェクトタブ・MIDI設定パネルの描画を確認

---

**Full Changelog**: `v1.9.9...v2.1.0`

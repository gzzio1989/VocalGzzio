# v2.0.0 — ハモリ大改修（ハモだけ修正・全9種選択・反行・エモート）

## 直したもの（バグ）

### 1. 「ハモだけ」で原音が消えていなかった ← 今回の本命
**原因は3つ重なっていました。**

1. **ユニゾン隊の2声がそのまま出ていた。** 5人ユニゾンの内訳は「本人±10セント程度のユニゾン隊2声＋ハモリ2声」。v1.9.9の「ハモだけ」はバッファを消してから4声全部を足し直していたので、本人とほぼ同じ音程のユニゾン隊が“原音の残り”として聴こえていました。
   → ハモリ音程を選んでいるときは**実際にハモっている2声だけ**を出します（2声になった分は+3dBで聴感を補正）。ユニゾンを選んでいるときは従来どおり4声＝重ね録り用ダブルとして使えます。
2. **Mixツマミが原音を混ぜ戻していた。** Mixが100%未満だと、消したはずの入力がそのまま混ざる経路が残っていました。
   → ハモだけON中はMixの原音側を自動で無効化。
3. **子音・息つぎで一瞬本人の声になっていた。** ピッチ検出が無声区間で0を返すたび、ハモリ音程が0半音（＝本人と同じ）へ落ちていました。
   → 直前の音程を保持して歌い続ける方式に変更（本物のコーラス隊が子音を同じ音程のまま歌うのと同じ挙動）。

**録音の位置合わせについて**: ハモだけの出力はプラグインが遅延補正値を正しく報告するので、DAWの自動補正で原音トラックとぴったり揃います。各メンバーの11〜31msの「アンサンブル感のズレ」は仕様（人間らしさ）です。

### 2. ハモリの選択肢が4つしか出ていなかった（表示と音のズレ）
DSP側はv1.9.8から8モードあったのに、プルダウンは4項目のまま。しかも並びがズレていて、
**「上5度」を選ぶと実際は3度下、「3度+5度」を選ぶと6度上が鳴っていました。**
→ 9モードすべてを正しい順で表示: ユニゾン / 上3度 / **下3度** / 上6度 / 下6度 / 上5度 / 3度+5度 / おまかせ / **反行(新規)**。
「3度下が選べない」問題はこれで解消です。

### 3. 文字が背景に沈んで読めない箇所
色をハードコードせず、**実際の背景色とのコントラスト比(WCAG 4.5:1)を計算して、足りなければ自動で濃く/明るく寄せる方式**に変更しました。対象: 配信音量メーターの説明文・ゾーン表示、「おまかせ設定」見出し(明るいモードで黄が沈んでいた)、ひびきの種類・EQ/エフェクト欄の説明文など。ヘッダーの「弾き…」「トー…」と切れていたシーンボタンも全文表示になりました。

### 4. プルダウンの漢字でフォントが変わる
ゆるかわ・自然モードの埋め込みフォント(Mochiy Pop One サブセット)に、v1.8以降に追加した文言の漢字が入っておらず、**195文字**がシステムフォントで代替表示されていました(拡声器の「拡」、付点の「付」、検出の「検」など)。
→ UIで使う全文字をソースから自動収集してサブセットを再生成(239KB→289KB)。再発防止用に `tools/make_font_subset.py` を同梱しました。文言に新しい漢字を足したら実行してください。
※ ▶ ⚠ ♭ ♯ ※ の5記号だけは元のフォント自体に無いため従来どおり代替表示です(記号なので違和感はalmost無し)。

## 追加したもの（機能）

### 5. 反行ハモリ（メロディと逆に動く対旋律）
上昇フレーズではハモリが下へ、下降フレーズでは上へ。合唱・ストリングスアレンジの基本技法です。
- 音階の度数で反対方向へ動き、開きすぎたら3度/6度へ静かに戻る
- 2声で成立する音程(3・4・5・7・8・9半音)以外は自動回避
- **検証: 12キー×8スケール×ランダム旋律で245,760回 → スケール外0件・濁る音程0件・反行/斜行率75.7%**
※ v1.9.8からあった「おまかせ」(高い音では下・低い音では上へ持ち替え)とは別物です。おまかせは平行、反行は逆方向。

### 6. エモート3機能（ポップス/バラード向け・全て0%なら音は完全に従来どおり）
- **息** (エフェクトタブ・空間欄): 小さい声のときだけ4.5kHz以上をそっと持ち上げる“逆ディエッサー”。ささやきに息づかいが乗り、耳元感が出ます。大声では動かないのでサ行は刺さりません。
- **エモ** (同上): ロングトーンを検出(約0.4秒〜)して、ひびきとひろがりをふわっと開きます。サビ終わりの伸ばした声だけが広がる「泣き」の演出。
- **サビリフト** (メイン画面・仕上げ欄): 直近の歌の強さ(1.2秒平均)が曲全体(8秒平均)を上回るとサビと判定し、ひろがり・かさね・やまびこ・ひびきをまとめて持ち上げ、Aメロで静かに戻します。プロがやる“サビでオートメーションを開く”の自動化です。

## 技術メモ
- 新パラメータ: `br_amt` / `emo_amt` / `lift_amt`(いずれも初期値0%)。`jn_harm` は選択肢を末尾に1つ追加(既存プロジェクトの保存値はそのまま有効)。
- 反行エンジンは JUCE 非依存の純関数(`gz::scale::ContraryLine`)として実装し、単体テストで検証済み。
- ハモリの方向判定の平滑化をブロック長非依存に修正(小バッファでも同じ反応速度)。
- 遅延は全機能で増えていません(検出系はすべて解析専用)。
